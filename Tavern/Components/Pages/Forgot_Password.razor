@page "/Forgot_Password"

<PageTitle>Forgot Password</PageTitle>

<div class="mainbody">

    <div class="campaign-container">
        <div class="campaign-item">
            <img src="imgs/Scrolls/Vector (3).png" alt="Scroll">
            <div class="text-overlay">
                <h1>Forgot Password?</h1>
                <p>Enter your email address here to get a password reset request</p>
                <input type="email" id="email" placeholder="Enter your email" class="email-input" />
                <a href="#viewcampaign" class="action-link">Sumbit</a>
            </div>
        </div>
    </div>

</div>


<style>
   html, body {
    margin: 0px;
    padding: 0px;
    }

    .mainheader {
        background-image: linear-gradient(rgba(20, 20, 17, 0.2), rgba(20, 20, 17, 1)), url("/imgs/campaign_manager_background.jpg");
        background-repeat: no-repeat;
        background-size: cover;
        background-position: center;  /* Center the background image */
        display: flex;                 /* Enable flexbox */
        justify-content: center;       /* Center horizontally */
        align-items: center;           /* Center vertically */
    }

    .mainheader-text {
        text-align: center;
        color: white;
        font-family: '';
        padding-bottom: 100px;
    }

    .mainbody {
        position: relative;
         /* Apply the background image and create a gradient overlay */
        background-image: radial-gradient(rgba(0, 0, 0, 0.0), rgba(0, 0, 0, 0.45)), url("/imgs/signup_background.png");
        /* Make sure the background covers the entire viewport */
        background-size: cover;
        background-position: center center;
        background-repeat: no-repeat;
        padding-top: 50px;
        padding-right: 50px;
        padding-bottom: 50px; 
    }

    .mainbody-container {
        opacity: 1;
        display: flex;
        flex-direction: column;
        align-items: left;
    }

    .mainbody-text {
        text-align: left;
        color: white;
        padding-left: 0%;
        text-shadow: 0px 4px 6px rgba(0, 0, 0, 1); /* Adding a shadow to the text */
        line-height: 260%;
    }

    /* Container for campaign images */
    .campaign-container {
        display: flex;  /* Use Flexbox to align items horizontally */
        justify-content: center;  /* Distribute the items with space in between */
        width: 100%;
        margin: 0 auto;
        padding-top: 50px;
        padding-bottom: 50px;
        padding-left: 50px;
        padding-right: 50px;
    }

    /* Each individual campaign item */
    .campaign-item {
        position: relative;  /* Allows positioning of the overlay on the image */
        width: 50%;  /* Adjust width to your liking (e.g., 30% for 3 items per row) */
        overflow: show;  /* Hide anything that overflows the container */
        text-shadow: 0px 0px 4px #612B24;
    }

    /* Style for images */
    .campaign-item img {
        width: 100%;  /* Make the image take up 100% of the container's width */
        height: auto;  /* Maintain aspect ratio */
        display: block;  /* Remove any space below the image */
    }

    /* The overlay container */
    .text-overlay {
        position: absolute;  /* Position the overlay on top of the image */
        top: 50%;  /* Center vertically */
        left: 50%;  /* Center horizontally */
        transform: translate(-50%, -50%);  /* Adjust for exact centering */
        color: #612B24;  /* Text color */
        background: rgba(0, 0, 0, 0);  /* Semi-transparent background for text readability */
        padding: 10px;
        text-align: center;
        width: 90%;  /* Adjust width of the overlay container */
        height: 90%;
        border-radius: 10px;
        padding-top: 25px;

        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .text-overlay h1 {
        margin: 0;
        /*font-size: 24px;*/
        font-size: 2vw;
        line-height: 2vw;
    }

    .text-overlay h2 {
        margin: 0;
        /*font-size: 24px;*/
        font-size: 1.5vw;
        line-height: 2vw;
    }

    .text-overlay p {
        margin-top: 10px;
        /*font-size: 16px;*/
        font-size: 1.25vw;
        line-height: 2vw;
    }

    /* Email input field styling */
    .email-input {
        margin-top: 20px;
        padding: 10px;
        width: 80%;  /* Makes the input field take up most of the container's width */
        font-size: 1vw;
        border: 2px solid #612B24;
        border-radius: 5px;
        background-color: #0000003f;
        color: #333;
    }

    .action-link {
        text-decoration: none;  /* Remove underline from links */
        color: #612B24;  /* Color of the links (you can change this to any color you prefer) */
        font-weight: bold;  /* Make the text bold */
        font-size: 18px;  /* Adjust the font size */
        padding: 20px;  /* Add some padding for a button-like look */  /* Add border around the link */
        border-radius: 5px;  /* Rounded corners */
        transition: all 0.3s ease;  /* Smooth transition effect */
        font-size: 1.25vw;
        line-height: 2vw;
    }

    .action-link:hover {
        color: white;  /* Change text color on hover */
        cursor: pointer;  /* Change cursor to pointer */
        text-shadow: 0px 0px 4px #000000;
    }

    .action-link:active {
        
    }

    /* Media query for smaller viewports */
    @@media (max-width: 1504px) {
        .text-overlay h1 {
            margin: 0;
            /*font-size: 24px;*/
            font-size: 1.75vw;
            line-height: 1vw;
        }

        .text-overlay h2 {
            margin: 0;
            /*font-size: 24px;*/
            font-size: 1.5vw;
            line-height: 1vw;
        }

        .text-overlay p {
            margin-top: 10px;
            /*font-size: 16px;*/
            font-size: 1.25vw;
            line-height: 1vw;
        }

        .action-link {
            font-size: 1.25vw;  /* Smaller font size */
            line-height: 1vw;  /* Smaller line height */
        }
    }

    /* Optional: For even smaller screens, you can have another media query */
    @@media (max-width: 1024px) {
        .action-link {
            font-size: 1.25vw;  /* Further smaller font size */
            line-height: 1.3;  /* Further smaller line height */
        }
    }

</style>